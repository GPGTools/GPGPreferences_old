{
    IBClasses = (
        {CLASS = FirstResponder; LANGUAGE = ObjC; SUPERCLASS = NSObject; }, 
        {
            CLASS = GPGPrefController; 
            LANGUAGE = ObjC; 
            OUTLETS = {initialFirstResponder = NSView; view = NSView; }; 
            SUPERCLASS = NSObject; 
        }, 
        {
            ACTIONS = {commentMatrixChanged = id; toggleVersion = id; updateComment = id; }; 
            CLASS = GPGSignaturePrefs; 
            LANGUAGE = ObjC; 
            OUTLETS = {
                commentMatrix = NSMatrix; 
                customCommentTextField = NSTextField; 
                versionSwitch = NSButton; 
            }; 
            SUPERCLASS = GPGPrefController; 
        }
    ); 
    IBVersion = 1; 
}